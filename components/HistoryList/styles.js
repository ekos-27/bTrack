import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerStyle: {
    flex: 1,
  },
  listStyle: {
    marginTop: 10,
    borderTopWidth: 4,
  },
  itemTitleStyle: {
  },
  leftIconStyle: {
  },
  buttonStyle: {
    position: 'absolute',
    top: 10,
    right: 10,
  },
});

export default styles;
