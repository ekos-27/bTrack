import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mapStyle: {
    position: 'absolute',
    top: 76,
    left: 0,
    right: 0,
    bottom: 0,
  },
  iconStyle: {
    fontSize: 24,
  },
});

export default styles;
